new Date(isoDate).getTime() ile ISO formatı timestamp'e çevirme
String temizleme ve Date parse ile özel format işleme
toLocaleDateString('tr-TR') ile okunabilir format
Date.now() - 3 * 24 * 60 * 60 * 1000 ile 3 gün geriye
Math.floor(Date.now() / 1000) - 259200 ile saniye formatı
new Date(timestamp).toLocaleString('tr-TR') ile tarih gösterimi
GitHub'a sürükle-bırak, /images klasörü veya harici link yöntemleri